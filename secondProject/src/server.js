const express = require('express')
const app = express() // app recebe
const port = 4000 // porta é um processo no computador

//ROTAS EXPRESS
/**
 * as rotas tem 4 partes
 * instância express
 * método de requisição (verbos)
 * função que será executada (middleware)
 */

